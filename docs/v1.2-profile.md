# CGA v1.2 P0 — Cold-index phase profile

- **Date:** 2026-05-20
- **Task:** v1.2 P0 — retire the v1.1 follow-up #1 ("profile cold index before
  the next batch") with concrete phase-time numbers.
- **Code:** `Config.profile_phases` + `_emit_phase_profile` in
  `src/cga/engine/graph_builder.py`. Enable with `CGA_PROFILE_PHASES=true`.

## What's instrumented

`build_graph_from_path_async` accumulates wall time per phase and writes a
breakdown to stderr when `Config.profile_phases` is on. Phases:

| phase | what it covers |
|---|---|
| `repository_node` | `add_repository_to_graph` MERGE |
| `discovery` | `rglob`, `index_ignore` filter, `.cgcignore` matcher |
| `pre_scan_imports` | `_pre_scan_for_imports` (builds the imports map) |
| `parse_file` | tree-sitter parse + Python AST walk per file |
| `add_file_to_graph` | per-file MERGEs: Files, Functions, Classes, Modules, IMPORTS, CONTAINS, parameters |
| `minimal_file_node` | placeholder File nodes for unparseable extensions |
| `inheritance_links` | `_create_all_inheritance_links` |
| `function_calls` | v1.1-batched CALLS resolution + UNWIND/MERGE flush |
| `unaccounted` | `total - sum(phases)` — sleeps, I/O waits, GC, etc. |

Off by default (no per-file overhead). When on, the timing reads add an
estimated <0.5 % per phase boundary.

## di-copilot run (scale test, 4 356 indexed files, 4 166.53 s wall ≈ 69 min)

```
files indexed: 2973
wall time:     4166.53 s
repository_node            0.00 s    0.0%     0.00 ms/file
discovery                 18.26 s    0.4%     6.14 ms/file
pre_scan_imports          57.34 s    1.4%    19.29 ms/file
parse_file               206.36 s    5.0%    69.41 ms/file
add_file_to_graph       1014.56 s   24.4%   341.26 ms/file
minimal_file_node         18.66 s    0.4%     6.28 ms/file
inheritance_links          1.11 s    0.0%     0.37 ms/file
function_calls          2800.78 s   67.2%   942.07 ms/file
unaccounted               49.47 s    1.2%
```

(2 973 here is the parsed-file count the profile uses as denominator; the
final graph has 4 356 `File` nodes once `minimal_file_node` placeholders
are counted. Wall time matches the v1.1 di-copilot baseline within ~12 %.)

### What changed from cgc-fork

The phase ratios moved hard. Per-file cost in DB-heavy phases exploded
while non-DB phases barely changed:

| phase | cgc-fork ms/file | di-copilot ms/file | growth |
|---|---:|---:|---:|
| parse_file | 40.01 | 69.41 | 1.7× |
| add_file_to_graph | 47.80 | 341.26 | **7.1×** |
| **function_calls** | 62.24 | 942.07 | **15.1×** |

Tree-sitter parse grew 1.7× (mostly larger files, expected). The two
DB-heavy phases grew 7-15× per file. **The dominant cost is graph-size
scaling of MERGE planning + execution on a populated FalkorDB graph, not
round-trip count, not file count.** This is the di-copilot signal the
v1.1 doc said we needed before picking the v1.2 P1 lever.

### Pivot from the cgc-fork-only verdict

The cgc-fork breakdown above tilted toward option (b) (batch
`add_file_to_graph`). The di-copilot data **walks that decision back**.
Per-file impact of attacking the two top phases:

- **Option (b) — `add_file_to_graph` batching:** if we get a v1.1-shaped
  50 % cut, that saves 12 % of total wall (~8 min of 69). Real but
  bounded.
- **Going after `function_calls` again:** even a 30 % cut here saves
  20 % of total wall (~14 min of 69). The bigger lever — but v1.1
  *already* batched this phase, so the remaining 67.2 % isn't
  round-trip count, it's something else inside
  `_create_all_function_calls`.

### The new question: what's actually slow inside `function_calls`?

`function_calls` covers three sub-phases v1.2 P0 didn't split:

1. The per-file Python resolution loop in `_create_function_calls` —
   building `called_name`, resolving `imports_map` / `local_names` /
   `local_imports`, appending to `_inscope_buf` / `_filescope_buf`.
2. `_flush_call_batches` threshold flushes (mid-loop, batches of 2000).
3. `_flush_call_batches` force flush at end.

Inside (2) and (3), each `session.run(_INSCOPE_BATCH_CYPHER, batch=head)`
runs a polymorphic 5-OPTIONAL-MATCH query against the live graph. As
the graph grows, that query's per-row cost grows even though the chunk
size stays at 2 000. v1.1's batching cut *round-trips* but not *per-row
graph-size cost*.

### v1.2 P0.5 — sub-profile inside `function_calls` (cgc-fork)

```
function_calls            10.63 s   32.0%    64.04 ms/file
  └─ resolve                0.28 s    2.6% of function_calls     1.70 ms/file
  └─ flush_inscope          9.55 s   89.9% of function_calls    57.55 ms/file
  └─ flush_filescope        0.79 s    7.5% of function_calls     4.78 ms/file
```

**89.9 % of `function_calls` is the in-scope UNWIND/MERGE Cypher.**
Python resolve is 2.6 %, file-scope flush 7.5 %. The cost lives inside
one specific `session.run(_INSCOPE_BATCH_CYPHER, ...)` per chunk —
which is the 5-`OPTIONAL MATCH` polymorphic COALESCE query.

If this ratio holds on di-copilot, the lever choice collapses:

- **Option (d) is ruled out** — pushing resolution out of the per-file
  loop saves at most ~3 % of `function_calls`, ~2 % of total wall.
  Not worth the design cost.
- **Option (c) is the lever** — simplify `_INSCOPE_BATCH_CYPHER`.
  Candidates: (i) split the in-scope template into Function-caller
  and Class-caller queries (two simpler 2-MATCH queries instead of one
  5-MATCH query per chunk); (ii) move the name-only fallback
  (`exact IS NULL → fbF`) to its own third flush so the common-case
  query has no `OPTIONAL MATCH (fbF) WHERE exact IS NULL` clause; (iii)
  index hints on `Function.path` + `Function.name` if FalkorDB
  supports plan hints.

The di-copilot P0.5 row below confirms this with one new twist —
`flush_filescope` is the secondary lever at scale, not the rounding
error cgc-fork made it look like.

### v1.2 P0.5 — sub-profile inside `function_calls` (di-copilot)

Fresh run on the same branch as this PR with `CGA_PROFILE_PHASES=true`.
4 356 indexed files, 4 134.0 s wall (≈ 69 min — within noise of the v1.1
baseline of 4 687.6 s, so the P0.5 instrumentation overhead is negligible).

```
function_calls          2803.04 s   67.8%   942.83 ms/file
  └─ resolve               16.93 s    0.6% of function_calls     5.70 ms/file
  └─ flush_inscope       2288.43 s   81.6% of function_calls   769.74 ms/file
  └─ flush_filescope      497.61 s   17.8% of function_calls   167.38 ms/file
```

**Verdict — c.ii is the lever, and it applies to both flush templates.**

- `flush_inscope` is **81.6 %** of `function_calls` and **55.4 % of total
  wall** (~38 min of 69). Same shape as cgc-fork — the in-scope polymorphic
  query is the dominant cost at every scale.
- `flush_filescope` is **17.8 %** of `function_calls` and **12.0 % of total
  wall** (~8 min of 69) — **2.4× the share it had on cgc-fork** (7.5 %).
  Because `_FILESCOPE_BATCH_CYPHER` carries the *same* `OPTIONAL MATCH
  (fbF:Function {name: item.called_name}) WHERE exact IS NULL` clause,
  the c.ii fallback-bucket split should bite there too. v1.2 P1b's scope
  expands to both templates.
- `resolve` is **0.6 %** of `function_calls` (~17 s) — pure Python
  resolution is essentially free, even at di-copilot scale. Option (d) is
  formally ruled out.

Parity check (edge counts vs v1.1 baseline):
`File=4 356`, `Function=19 085`, `Class=3 134`, `Module=2 870`,
`Repository=1`, `Directory=606`, `CONTAINS=103 599`, `CALLS=91 459`,
`INHERITS=79`, `IMPORTS=17 215`, `HAS_PARAMETER=20 661`. **All identical**
— P0.5 instrumentation does not perturb graph output. PASS gate #4 (edge
parity at scale) is met for the profile-enabled cold-index path.

`add_file_to_graph` rerun: 987.07 s, **23.9 %** of total wall, 332.01
ms/file (cgc-fork: 24.4 %, 47.80 ms/file). The 24 % ratio is stable across
both scales — locks in P1a as the floor batching target.

## Decision for v1.2 P1 (di-copilot P0.5 lands the verdict)

The cgc-fork-only P0 verdict pointed at option (b) — batch
`add_file_to_graph`. The cgc-fork P0.5 sub-split pivoted that to a
sub-profile pass first. The di-copilot P0.5 row now closes the question:

- **(b′) — batch `add_file_to_graph`. Lock in as P1a.** 24.4 % cgc-fork,
  23.9 % di-copilot. Stable per-file ratio across scales is the highest-
  confidence batch target on the board. Same UNWIND/MERGE pattern as v1.1.
- **(c.ii) — fallback-bucket split. Lock in as P1b.** `flush_inscope` is
  81.6 % of `function_calls` and 55.4 % of total wall on di-copilot.
  `flush_filescope` is **a second, growing lever** — 17.8 % on di-copilot
  vs 7.5 % on cgc-fork (2.4× the share). Both templates carry the same
  `OPTIONAL MATCH (fbF) WHERE exact IS NULL` fallback clause, so the c.ii
  split applies to **both** — pre-classify each spec in Python by whether
  resolution returned a real `called_path`, then send resolved specs through
  a fallback-free template and unresolved specs through a name-only
  template. Cascade priority preserved by construction.
- **(d) — resolve work in pre-built lookup. Ruled out.** 0.6 % of
  `function_calls` (~17 s of 4 134 s). Not worth the design cost.

See `_local-archive/handoffs/eng-review-v1.2-p1-2026-05-20.md` for the
full sub-option analysis. P1c (FalkorDB index hints) is deferred —
conditional on P1b under-delivering.

## cgc-fork run (small Python-heavy repo, 364 files, 32.54 s wall)

```
files indexed: 166
wall time:     32.54 s
repository_node            0.00 s    0.0%     0.01 ms/file
discovery                  0.20 s    0.6%     1.21 ms/file
pre_scan_imports           1.18 s    3.6%     7.13 ms/file
parse_file                 6.64 s   20.4%    40.01 ms/file
add_file_to_graph          7.94 s   24.4%    47.80 ms/file
minimal_file_node          2.08 s    6.4%    12.53 ms/file
inheritance_links          0.05 s    0.2%     0.30 ms/file
function_calls            10.33 s   31.7%    62.24 ms/file
unaccounted                4.12 s   12.7%
```

(`ms/file` denominator is the 166 parsed-file count, not all 364 — so
`minimal_file_node`'s 12.53 ms/file is per *parseable* file, not per
unparseable one it actually serves.)

## Findings

### 1. `function_calls` is still the largest phase even after v1.1 batching

10.33 s of 32.54 s — 31.7 % — is the v1.1-batched CALLS pass. The v1.1 plan
collapsed 200 k–450 k round-trips down to ~46 chunks. That win is locked in.
What's left in this bucket isn't round-trip count — it's the cost of the
polymorphic COALESCE query *per chunk*. Each batch evaluates five
`OPTIONAL MATCH` clauses and a `WHERE caller IS NOT NULL AND called IS NOT
NULL` filter against an N-node graph; the plan-time and per-row cost may
grow with N.

**Next probe:** instrument *inside* `_flush_call_batches` to split flush
wall-time from resolution wall-time. If flush dominates, the polymorphic
query shape is the lever (split the cascade into 2 simpler queries; drop
the name-only fallback to an explicit second flush). If resolution
dominates, the per-call Python loop is the lever.

### 2. `add_file_to_graph` is the obvious next batching target (option b from the v1.1 doc)

7.94 s, 24.4 %, 47.80 ms/file. This phase holds the per-file MERGEs for
`File`, `Function`, `Class`, `Module`, `IMPORTS`, and `CONTAINS` — and
unlike CALLS, none of these were batched in v1.1. Each indexed file
issues many MERGEs serially. cgc-fork has 8 042 `CONTAINS` and 691
`IMPORTS` — di-copilot has 103 599 / 17 215. Batching this phase by the
same UNWIND/MERGE pattern v1.1 used should move the needle.

**Caveat — the v1.1 anti-pattern:** the same logic ("CALLS round-trips
dominate") under-delivered on di-copilot because the per-call cost wasn't
just round-trip overhead. Before committing to batching
`add_file_to_graph`, **re-profile against di-copilot** to confirm the same
phase ratio at scale. The cgc-fork breakdown is suggestive, not
definitive; the di-copilot run may show a very different shape.

### 3. `parse_file` (tree-sitter) is third at 20.4 %

40 ms/file on cgc-fork. Hard to optimize without rewriting against
different grammars or running parsers in parallel. **Low-priority lever**
unless di-copilot's TypeScript-heavy frontend changes the mix.

### 4. `asyncio.sleep(0.01)` per file is ~11 % of cgc-fork wall time

`build_graph_from_path_async` does `await asyncio.sleep(0.01)` after
every file. 364 files × 10 ms = 3.64 s of pure overhead — almost the
entire 4.12 s `unaccounted` bucket. The sleep exists to yield the event
loop so concurrent watchers/queries don't starve; `await asyncio.sleep(0)`
or removing it entirely on the cold path would recover all of this. On
di-copilot at 4 356 files × 10 ms = 43.6 s (about 1 % of the 4 687 s wall),
so it's a cgc-fork-shaped win and a di-copilot-shaped rounding error —
worth doing on the merits, not as a v1.2 P1 lever.

## Follow-up plan (locked in after P0.5)

1. **This PR (v1.2 P0 + P0.5)** — phase-timer scaffold + cgc-fork +
   di-copilot top-level breakdowns + cgc-fork and di-copilot
   sub-profiles inside `function_calls`. No engine logic change.
2. **v1.2 P1a** — batch `add_file_to_graph`. Same UNWIND/MERGE buffered-
   flush pattern v1.1 used for CALLS. Floor target (~24 % phase share
   on both proving repos).
3. **v1.2 P1b** — c.ii fallback-bucket split, applied to both
   `_INSCOPE_BATCH_CYPHER` and `_FILESCOPE_BATCH_CYPHER`. Pre-classify
   resolved vs. unresolved specs in Python; two purpose-built queries
   per template, neither carries the `OPTIONAL MATCH (fbF) WHERE exact
   IS NULL` clause.
4. **v1.2 P1c (conditional)** — FalkorDB index hints, only if P1b
   under-delivers vs the cgc-fork extrapolation.

## Non-goals for this PR

- No engine logic change. Instrumentation only.
- No di-copilot run (the next session does it).
- No removal of `asyncio.sleep(0.01)` — captured as a follow-up but not
  shipped here.
- No comparison against CGC v0.3.1 internals. Separate research task.

## Files touched

- `src/cga/config.py` — `profile_phases: bool = False` + `CGA_PROFILE_PHASES` env.
- `src/cga/engine/graph_builder.py` — phase timers around the seven phases
  above + `_emit_phase_profile` helper. All gated by
  `Config.profile_phases`; default path is unchanged.
- `docs/v1.2-profile.md` — this file.
